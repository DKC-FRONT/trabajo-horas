-- Migration reference: copy of setup.sql (apply via deploy script or supabase CLI)
-- NOTE: This is a snapshot for version control. The canonical file is supabase/setup.sql

-- If you want to run directly with psql use: psql "$DATABASE_URL" -f supabase/setup.sql
-- Kept intentionally minimal to avoid duplication in documentation.

-- BEGIN REF: supabase/setup.sql
-- (See supabase/setup.sql in the repo root for the full script)
-- END REF
